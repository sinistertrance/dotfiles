# https://github.com/chick2d/neofetch-themes
# Made by https://github.com/chatsagnik

print_info() {
    #prin " "
    info "$(color 6) " distro
    info "$(color 6) " kernel
    info "$(color 6) " uptime
    info "$(color 6) " packages
    #info "$(color 6)  CPU" cpu
    #info "$(color 6)  GPU" gpu
    info "$(color 6) " memory
    info "$(color 1) $(color 2) $(color 3) $(color 4) $(color 5) $(color 6) $(color 7) $(color 8)"
    info cols
}

title_fqdn="off"

kernel_shorthand="on"

distro_shorthand="off"

os_arch="off"

uptime_shorthand="tiny"

memory_percent="off"

memory_unit="gib"

package_managers="on"

shell_path="off"

shell_version="off"

speed_type="bios_limit"

speed_shorthand="on"

cpu_brand="off"

cpu_speed="on"

cpu_cores="logical"

cpu_temp="off"

gpu_brand="on"

gpu_type="dedicated"
#gpu_type="all"

refresh_rate="on"

gtk_shorthand="off"

gtk2="on"

gtk3="on"

de_version="on"

colors=(distro)

bold="on"

underline_enabled="on"

underline_char="-"

separator=":"

block_range=(0 15)

color_blocks="on"

block_width=3

block_height=1

col_offset="auto"

bar_char_elapsed="-"
bar_char_total="="

bar_border="on"

bar_length=15

bar_color_elapsed="distro"
bar_color_total="distro"


cpu_display="on"
memory_display="off"
battery_display="off"
disk_display="off"


#image_backend="kitty"

image_source="auto"


ascii_distro="fedora_small"

ascii_colors=(distro)

ascii_bold="on"

image_loop="off"

#thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch"

crop_mode="normal"

crop_offset="center"

image_size="auto"

gap=3

yoffset=0
xoffset=0

